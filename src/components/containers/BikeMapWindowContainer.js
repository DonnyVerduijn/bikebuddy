import { connect } from 'react-redux';
import BikeMapWindow from './../app/BikeMapWindow';

const mapStateToProps = () => {};

const mapDispatchToProps = () => {};

export default connect(
  mapStateToProps,
  mapDispatchToProps,
)(BikeMapWindow);
